[tox]
envlist = py{27,35,36,37}

[testenv]
platform =
    windows: win32
    linux: linux
    darwin: darwin
deps =
    pytest
    #pytest-cov
    #coverage
    Cython  # >0.29.13
passenv = *
commands =
    python -m pytest fastrlock/tests --capture=no --strict {posargs}
